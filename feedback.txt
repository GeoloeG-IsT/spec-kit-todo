Workflow:
- commit after each task (might be in constitution.md?)
- be sure to have latest version of tools and libraries installed

Backend:
- check python version and install with python version manager like pyenv
- no virtual environment was setup
- pip was used instead of uv or poetry
- sqlalchemy used in sync with sqlmodel (see if using context 7 might lead to better understanding of SQLModel)
- be precise for test/mock strategy

Frontend:
- check node version and install with node version manager like nvm
- npm was used instead of pnpm or yarn or bun
- be precise for test/mock strategy

Database:
- neon was used instead of supabase or any other database


Claude:
- $HOME is not readable by claude ?
